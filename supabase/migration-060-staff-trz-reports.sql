-- ============================================================
-- Migration 060 — „Влиза в ТРЗ справките" (по СЛУЖИТЕЛ)
-- ============================================================
-- Управителят иска да отбелязва отпуска и дистанционна работа в
-- Календара, за да ги виждат колегите — но БЕЗ да влиза във Форма 76 и
-- Справка отпуска. Другият колега от „Управление" остава в тях.
--
-- Затова признакът е НА НИВО ЧОВЕК, а не на отдел: отделът „Управление"
-- има двама души с различно отношение към отчетността.
--
-- Полето е ПОЛОЖИТЕЛНО с default true → всички заварени служители
-- остават както са сега. Изключва се само този, който бъде отметнат.
--
-- Отсъствията му си остават в crm_absences и се виждат в Календара —
-- това е целта. Изключването е само от двете ТРЗ справки и от месечния
-- Excel експорт на Календара (той също е справка за заплати).
--
-- Идемпотентно.
-- ============================================================

alter table crm_staff
  add column if not exists in_trz_reports boolean not null default true;

NOTIFY pgrst, 'reload schema';
