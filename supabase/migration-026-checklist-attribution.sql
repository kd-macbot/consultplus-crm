-- ============================================================
-- Migration 026 — Чек лист: атрибуция „кой какво маркирал"
-- ============================================================
-- Добавя JSONB колона checked_by на crm_checklist: за всяка от 12-те
-- чек-стъпки пазим кой я е маркирал и кога:
--   { "check_clients": { "name": "Иван Иванов", "at": "2026-06-17T..." }, ... }
--
-- Така при споделен ред счетоводител и отговорник виждат кой е отметнал
-- всяка стъпка (показва се при hover на чекбокса).
--
-- Идемпотентно: пуска се повторно без грешка.
-- ============================================================

alter table crm_checklist
  add column if not exists checked_by jsonb not null default '{}'::jsonb;
