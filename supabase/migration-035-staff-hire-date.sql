-- ============================================================
-- Migration 035 — Дата на назначаване на служители
-- ============================================================
-- Добавя поле „hire_date" на crm_staff — основа за изчисление на стаж
-- (тенюр). Ще се ползва от:
--   1. Карти в Персонал — „Стаж: 2 години 4 месеца"
--   2. Бъдещ модул „Придобивки" — еligibility per служител (10/20/30 дни
--      отпуска, бонуси и т.н. на базата на стажа)
--
-- nullable: старите служители ще се попълнят от admin постепенно.
-- Идемпотентно.
-- ============================================================

alter table crm_staff
  add column if not exists hire_date date;
