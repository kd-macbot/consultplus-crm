-- ============================================================
-- Migration 052 — КИ (кредитни известия) към СПО справката
-- ============================================================
-- Фирмени месечни стойности 20% и 9%, като „фактури в брой", но се
-- ПРИБАВЯТ към СПО (фактурите се изваждат):
--   СПО 20% = (Общо20 − Сторно20) − Фактури 20% − РЗОК + КИ 20%
--   СПО 9%  = (Общо9  − Сторно9)  − Фактури 9%  + КИ 9%
--
-- Идемпотентно (add column if not exists).
-- ============================================================

alter table crm_cash_firm_monthly add column if not exists credit_note_20 numeric not null default 0;
alter table crm_cash_firm_monthly add column if not exists credit_note_9 numeric not null default 0;
