-- ============================================================
-- Migration 027 — Персонал: допълнителни отдели
-- ============================================================
-- Добавя масив additional_departments на crm_staff. Така един служител
-- може да участва в няколко отдела — напр. тийм лийд (основен отдел
-- „Тийм Лийд") да е избираем и като „Счетоводител", ако има и
-- „Счетоводство" в допълнителните си отдели.
--
-- getStaff(dept) връща служители, чийто ОСНОВЕН или ДОПЪЛНИТЕЛЕН отдел
-- съвпада с dept → появяват се в съответния dropdown (Счетоводител и т.н.).
--
-- Идемпотентно: пуска се повторно без грешка.
-- ============================================================

alter table crm_staff
  add column if not exists additional_departments text[] not null default '{}'::text[];
