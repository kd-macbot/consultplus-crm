-- ============================================================
-- Migration 039 — Банков достъп: код за 2FA приложение
-- ============================================================
-- Добавя поле app_code — таен код/сийд за автентикиращото приложение
-- (различно от парола за вход). Незадължително — не всички клиенти
-- имат 2FA app. Masked в UI като паролата.
--
-- Идемпотентно.
-- ============================================================

alter table crm_bank_access
  add column if not exists app_code text;
