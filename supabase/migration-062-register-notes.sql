-- ============================================================
-- Migration 062 — бележка по касов апарат
-- ============================================================
-- Колоната стои в таблицата „Годишен оборот" (след КА / Сторно / КО) —
-- мястото на празната колона от excel-а на колегите.
--
-- Бележката е НА АПАРАТА, не на годината: видима е във всяка година и не
-- се преписва наново всеки януари (решение на потребителя). Ако някога
-- потрябва бележка за конкретна година, тя е ДРУГО поле — това не се
-- преправя, защото ще загуби вече написаното.
--
-- Идемпотентно, additive.
-- ============================================================

alter table crm_cash_registers
  add column if not exists notes text;

NOTIFY pgrst, 'reload schema';
